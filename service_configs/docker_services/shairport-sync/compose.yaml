services:
  shairport:
    image: mikebrady/shairport-sync:4.3.7
    container_name: shairport
    network_mode: host
    restart: unless-stopped
    volumes:
      - /opt/shairport-sync/shairport-sync.conf:/etc/shairport-sync.conf:ro
      - ${APPDATA_ROOT}/shairport_data/fifo:/fifo
      - /var/run/dbus:/var/run/dbus

  snapserver:
      image: sweisgerber/snapcast
      container_name: snapserver
      network_mode: host
      restart: unless-stopped
      volumes:
        - /opt/snapserver/snapserver.conf:/config/snapserver.conf
        - ${APPDATA_ROOT}/shairport_data/fifo:/fifo:ro
        - ${APPDATA_ROOT}/snapserver_data:/data
      depends_on:
        - shairport
