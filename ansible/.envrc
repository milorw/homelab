# .envrc - local-only Ansible vault config
vault_script="$(expand_path ./vault_pass_1password.sh)"

if [[ -x "$vault_script" ]]; then
  export ANSIBLE_VAULT_PASSWORD_FILE="$vault_script"
else
  log_status "vault_pass_1password.sh missing or not executable - vault disabled"
fi
